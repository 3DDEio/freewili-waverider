# FreeWili 2 RP2350B Display controller through the built-in CMSIS-DAP probe.
# OpenOCD must include the Raspberry Pi rp2xxx flash driver and rp2350 target.
source [find interface/cmsis-dap.cfg]
cmsis-dap usb interface 0
adapter speed 5000
source [find target/rp2350.cfg]
